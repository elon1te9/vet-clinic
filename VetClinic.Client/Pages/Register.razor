@page "/register"
@inject VetClinic.Client.Services.AuthService AuthService
@inject NavigationManager Navigation

<PageTitle>Регистрация</PageTitle>

<h1>Регистрация</h1>

<div>
    <label>ФИО</label>
    <input @bind="fullName" placeholder="Введите ФИО" />
</div>

<div>
    <label>Email</label>
    <input @bind="email" placeholder="Введите email" />
</div>

<div>
    <label>Пароль</label>
    <input type="password" @bind="password" placeholder="Введите пароль" />
</div>

<div>
    <label>Телефон</label>
    <input @bind="phoneNumber" placeholder="Введите телефон" />
</div>

<div>
    <label>Адрес</label>
    <input @bind="address" placeholder="Введите адрес" />
</div>

@if (!string.IsNullOrWhiteSpace(errorMessage))
{
    <p>@errorMessage</p>
}

<button @onclick="RegisterUser" type="button">Зарегистрироваться</button>

@code {
    private string fullName = string.Empty;
    private string email = string.Empty;
    private string password = string.Empty;
    private string phoneNumber = string.Empty;
    private string address = string.Empty;
    private string errorMessage = string.Empty;

    private async Task RegisterUser()
    {
        if (string.IsNullOrWhiteSpace(fullName) ||
            string.IsNullOrWhiteSpace(email) ||
            string.IsNullOrWhiteSpace(password))
        {
            errorMessage = "Заполните все поля.";
            return;
        }

        errorMessage = string.Empty;

        var result = await AuthService.RegisterOwnerAsync(new VetClinic.Shared.Requests.RegisterOwnerRequest
        {
            FullName = fullName,
            Email = email,
            Password = password,
            PhoneNumber = phoneNumber,
            Address = address
        });

        if (result is null)
        {
            errorMessage = "Не удалось выполнить регистрацию.";
            return;
        }

        Navigation.NavigateTo("/");
    }
}
