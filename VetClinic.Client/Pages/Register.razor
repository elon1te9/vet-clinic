@page "/register"
@inject VetClinic.Client.Services.AuthService AuthService
@inject NavigationManager Navigation

<PageTitle>Регистрация</PageTitle>

<div class="auth-page">
    <div class="auth-card">
        <PageHeader Title="Регистрация" Description="Создайте аккаунт владельца питомца." />

        <div>
            <label>ФИО</label>
            <input @bind="fullName" autocomplete="name" />
        </div>

        <div>
            <label>Электронная почта</label>
            <input @bind="email" autocomplete="email" />
        </div>

        <div>
            <label>Пароль</label>
            <input type="password" @bind="password" autocomplete="new-password" />
        </div>

        <div>
            <label>Телефон</label>
            <input @bind="phoneNumber" autocomplete="tel" />
        </div>

        <div>
            <label>Адрес</label>
            <input @bind="address"/>
        </div>

        <ErrorMessage Text="@errorMessage" />

        <div class="actions-row">
            <button class="btn btn-primary" @onclick="RegisterUser" type="button">Создать аккаунт</button>
            <a class="btn btn-secondary" href="login">Уже есть аккаунт</a>
        </div>
    </div>
</div>

@code {
    private string fullName = string.Empty;
    private string email = string.Empty;
    private string password = string.Empty;
    private string phoneNumber = string.Empty;
    private string address = string.Empty;
    private string errorMessage = string.Empty;

    private async Task RegisterUser()
    {
        if (string.IsNullOrWhiteSpace(fullName) ||
            string.IsNullOrWhiteSpace(email) ||
            string.IsNullOrWhiteSpace(password))
        {
            errorMessage = "Заполните ФИО, электронную почту и пароль.";
            return;
        }

        errorMessage = string.Empty;

        var result = await AuthService.RegisterOwnerAsync(new VetClinic.Shared.Requests.RegisterOwnerRequest
        {
            FullName = fullName,
            Email = email,
            Password = password,
            PhoneNumber = phoneNumber,
            Address = address
        });

        if (result is null)
        {
            errorMessage = "Не удалось сохранить данные. Проверьте заполнение формы.";
            return;
        }

        Navigation.NavigateTo("/", true);
    }
}
